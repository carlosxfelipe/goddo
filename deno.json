{
  "workspace": [
    "./packages/core",
    "./packages/bearer",
    "./packages/cors",
    "./packages/cron",
    "./packages/csrf",
    "./packages/html",
    "./packages/jwt",
    "./packages/llms-txt",
    "./packages/openapi",
    "./packages/rate-limit",
    "./packages/server-timing",
    "./packages/shield",
    "./packages/static",
    "./packages/treaty"
  ],
  "tasks": {
    "dev": "deno run --watch --allow-net --allow-env src/index.ts",
    "start": "deno run --allow-net --allow-env src/index.ts",
    "test": "deno test --allow-net --allow-read --allow-env",
    "check": "deno check packages/core/index.ts",
    "fmt": "deno fmt",
    "lint": "deno lint",
    "bench": "deno bench --allow-net benchmarks/",
    "coverage": "rm -rf cov/ && deno test --coverage=cov/ --allow-net --allow-read --allow-env && deno coverage cov/",
    "init": "deno run --allow-read --allow-write scripts/init.ts"
  },
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true,
    "noUncheckedIndexedAccess": true,
    "jsx": "react-jsx",
    "jsxImportSource": "@goddo/html"
  },
  "fmt": {
    "lineWidth": 100,
    "indentWidth": 2,
    "singleQuote": true,
    "semiColons": false
  },
  "lint": {
    "rules": {
      "tags": [
        "recommended"
      ]
    }
  },
  "exclude": [
    "**/node_modules",
    "bruno/"
  ]
}
